"""
Import the User class from the file user.py located in the same models package. 
This allows us to use the User model in other parts of the application by importing it from the models package.
"""
from .user import User